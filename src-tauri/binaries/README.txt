Optional dev-only: Tauri can use a prebuilt sidecar EXE named paradox-api-x86_64-pc-windows-msvc.exe here.
Release installers bundle the PyInstaller tree from ../dist/paradox-api/ (see tauri.conf.json bundle.resources).
